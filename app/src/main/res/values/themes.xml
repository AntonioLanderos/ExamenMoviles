<resources>

    <!-- Base application theme -->
    <style name="Theme.ExamenMoviles" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Personalización de tu tema -->
        <item name="colorPrimary">@color/black</item> <!-- Color principal negro -->
        <item name="colorPrimaryVariant">@color/orange_dragon_ball</item> <!-- Variante naranja -->
        <item name="colorSecondary">@color/blue_dragon_ball</item> <!-- Azul -->
        <item name="android:statusBarColor">@color/black</item> <!-- Barra de estado negra -->
        <item name="android:navigationBarColor">@color/black</item> <!-- Barra de navegación negra -->

        <!-- Colores del texto -->
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
    </style>

</resources>